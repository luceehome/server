extends layout

block content
  h1= title
  p Welcome to #{title}

  form(name='config-form')
    div
      label Set fhem location of the fhem server
      input(class='config-fhem-location', placeholder='e.g. localhost:8083')
